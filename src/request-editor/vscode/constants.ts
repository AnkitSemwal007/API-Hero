/** Re-export shared view type; debounce stays request-editor local. */
export { REQUEST_EDITOR_VIEW_TYPE } from '../../constants';

/** Default debounce for form ↔ document sync (ms). */
export const REQUEST_EDITOR_SYNC_DEBOUNCE_MS = 300;
