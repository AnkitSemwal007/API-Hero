# Create a bearer profile named "demo" in Manage Authentication,
# store the token in Secret Storage, then run this request.

@auth demo

### Authenticated ping
GET https://httpbin.org/bearer
Accept: application/json
